# Studienarbeit Adrian Gabriel
This repository contains the experiments of the baseline models (folder named "baseline_models") and the basic transformer (folder named "transformer_models").
You will find a directory containing the results as plots for each of the models ("team_a")
